<!-- a.component.html -->

<div class="container mt-4">
  <div class="row">
    <!-- Left Column (Size 4) -->
    <div class="col-md-4">
      <h2>Menu</h2>
      <div class="list-group">
        <a class="list-group-item" (click)="setActiveTab('content1')">Home</a>
        <a class="list-group-item" (click)="setActiveTab('content2')">B Component</a>
        <a class="list-group-item" (click)="setActiveTab('content3')">C Component</a>
      </div>
    </div>

    <!-- Right Column (Size 8) -->
    <div class="col-md-8">
      <h2>Navbar with Tabs</h2>
      <ul ngbNav #nav="ngbNav" [(activeId)]="activeTab" class="nav-tabs">
        <li *ngFor="let id of navs" [ngbNavItem]="id">
          <button ngbNavLink>
            Tab {{ id }}
            <span class="btn-close ms-3 fw-light" (click)="close($event, id)"></span>
          </button>
          <ng-template ngbNavContent>
            <!-- Content for Tab {{ id }} -->
            <div *ngIf="id === 'content1'">
              <p>Content of Tab 1 goes here.</p>
            </div>
            <div *ngIf="id === 'content2'">
              <app-b></app-b>
            </div>
            <div *ngIf="id === 'content3'">
              <app-c></app-c>
            </div>
          </ng-template>
        </li>
      </ul>
    </div>
  </div>
</div>
